name = "panuki-store-client"
compatibility_date = "2026-09-15"

[assets]
directory = "./dist"
# Sirve index.html para cualquier ruta no encontrada, para que /admin/login o
# /admin/productos/:id funcionen al entrar directo o refrescar (SPA con vue-router
# en modo history).
not_found_handling = "single-page-application"
